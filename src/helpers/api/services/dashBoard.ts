import { APICore } from "../apiCore";


const api = new APICore();

export const getOccupencyData = (params: any = null) => {
    const baseUrl = ``;
    return api.get(baseUrl, params);
};
export const getEnvironmentConditionsData  = (params: any = null) => {
    const baseUrl = ``;
    return api.get(baseUrl, params);
};
export const getLocationsplitData  = (params: any = null) => {
    const baseUrl = ``;
    return api.get(baseUrl, params);
};
export const getTableData  = (params: any = null) => {
    const baseUrl = ``;
    return api.get(baseUrl, params);
};
export const getDashboardStats  = (params: any = null) => {
    const baseUrl = ``;
    return api.get(baseUrl, params);
};


 
 